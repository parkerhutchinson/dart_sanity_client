name: dart_sanity_client
description: A complete sanity.io http client. includes queries in grok and graphql. Has methods for fetch, create, patch, and publish.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.6.0

# Add regular dependencies here.
dependencies:
  http: ^1.2.2

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
  dotenv: ^4.2.0
